# Playground

Small Rust library used to test the pi agent.

## Rules
- Fix bugs with minimal changes — do not rewrite working code
- Every new public function needs a `///` doc comment
- Never use `unwrap()` — use `expect("reason")` or `?`
