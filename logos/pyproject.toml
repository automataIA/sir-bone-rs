[project]
name = "pi-logos"
version = "0.1.0"
description = "Braille animation tools — GIF/WebP → terminal + binary BRLF export for pi-tui"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.2.0",
]
